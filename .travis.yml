#
# .travis.yml
# Configuration for CI
#

language: python
python:
    - "3.6"
before_install:
    - sudo apt-get update
    - sudo apt-get install python3
install:
    - pip install -r requirements.txt
script:
    pytest

