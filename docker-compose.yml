version: '1'

services:
    node:
        build: .
        image: networks/Dockerfile.node
        expose:
            - "30303"
        volumes:
            - connection_data:/network/chain/tmp

volumes:
    connection_data:
        driver: local
        driver_opts:
            type: tmpfs
            device: tmpfs
            o: "size=10m"

